# Relative Position-Aware Self-Attention

**currently under development**

### Installation

- Requirements:
    - `torch`
    - C++ compiler (gcc, MSVC)
    - CUDA compiler (nvcc)

- Commands:

```terminal
cd rpa_sa
python setup.py build_ext -i
```
