# Relation-Aware Self-Attention

**currently under development**

### Installation

- Requirements:
    - `torch`
    - C++ compiler (gcc, MSVC)
    - CUDA compiler (nvcc)

- Commands:

```terminal
cd rasa
python setup.py build_ext -i
```
